{
  "name": "@ai/tools",
  "type": "module",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "dev": "tsx ./src/index.ts",
    "type-check": "tsc --noEmit",
    "lint": "eslint --fix",
    "test": "vitest",
    "build": "tsc && tsc-alias",
    "debug": "tsc && tsc-alias -p tsconfig.json --debug"
  },
  "devDependencies": {
    "@ai/typescript-config": "workspace:*",
    "vitest": "catalog:test"
  }
}
